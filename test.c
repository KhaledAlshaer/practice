#include "header.h"


typedef enum {
        SemiColon,
	OPEN_PAREN,
	CLOSE_PAREN,
	OPEN_CURLY_PAREN,
	CLOSE_CURLY_PAREN,
} Seperators;

typedef enum{
	RETURN,
	INT,
} Keywords;


typedef enum {
	LITERAL_INT,
} Literals;


/**
 *
 * comment section
 *
 */
int main() {
	return (2);
}
